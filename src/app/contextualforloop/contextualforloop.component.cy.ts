import { ContextualforloopComponent } from './contextualforloop.component'

describe('ContextualforloopComponent', () => {
  it('should mount', () => {
    cy.mount(ContextualforloopComponent)
  })
})